import Head from 'next/head';
import Layout, { siteTitle } from '../components/layout';
import utilStyles from '../styles/utils.module.css';
import { getSortedPostsData } from '../lib/posts';
import Link from 'next/link';
import Date from '../components/date';


export async function getStaticProps() {
  const allPostsData = getSortedPostsData();
  return {
    props: {
      allPostsData,
    },
  };
}

export default function Home({ allPostsData }) {
  return (
    <Layout home>
      <Head>
        <title>{siteTitle}</title>
      </Head>
      <section className={utilStyles.headingMd}>
        <h1>Welcome to Next.JS</h1>
        <p>Hey, My self Mohammad Abbas. I am a passionate web developer and programmer. Tech enthusiast</p>
        <p>You can connect with me on <a href='www.linkedin.com/in/mohammad-abbas-b41828270 '>LinkdIn</a></p>
        <p>
          Innovative Frontend Developer || Expert in Modern Technologies || Transforming Visions into Immersive Digital Experiences || HTML || CSS || TypeScript || Node Js || React Js || Next Js || Tailwind CSS || Block-Chain
        </p>
      </section>
      <section className={`${utilStyles.headingMd} ${utilStyles.padding1px}`}>
        <h2 className={utilStyles.headingLg}>Blog</h2>
        <ul className={utilStyles.list}>
          {allPostsData.map(({ id, date, title }) => (
            <li className={utilStyles.listItem} key={id}>
              <Link href={`/posts/${id}`}>{title}</Link>
              <br />
              <small className={utilStyles.lightText}>
                <Date dateString={date} />
              </small>
            </li>
          ))}
        </ul>
      </section>
    </Layout>
  );
}